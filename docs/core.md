# Core Package 🔧

Should include init and core CLI features.
This includes configuration loading ,validations, os permissions etc.
The core package is mainly responsible for the project intiation for now.

Some of those features are mentioned in the next section.

## Core features

- [ ] `init` - Create new React project
  - [ ] JavaScript / TypeScript selection
  - [ ] Styling method (CSS / SCSS / Tailwind / styled-components)
  - [ ] Routing setup (React Router)
  - [ ] State management (Redux / Zustand / etc.)
  - [ ] ESLint + Prettier setup
  - [ ] Testing framework (Jest / RTL / Cypress)
  - [ ] Project structure templates (`feature-based`, `atomic`, etc.)
